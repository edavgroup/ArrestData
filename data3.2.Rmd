```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
library(RSocrata)
library(ggplot2)
library(tidyverse)
```

```{r}
data =  read.socrata("https://data.cityofnewyork.us/Public-Safety/NYPD-Arrest-Data-Year-to-Date-/uip8-fykc")
```

```{r}
head(data)
```

```{r}
data = data %>% select(-c(geocoded_column))
```

We drop the column called 'geocoded_column', since this column is a combination of longitude coordinate as x coordiante and latitude cooridnate as y coordinate.

```{r}

```

